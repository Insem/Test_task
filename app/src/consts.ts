export const REPLICA_ID = Date.now();
export const REPLICA_COUNT = Number(process.env.REPLICA_COUNT) || 5;
export const RPC_QUEUE_BALANCE = "rpc_queue_balance";
export const RPC_QUEUE_TASKS = "rpc_queue_tasks";
